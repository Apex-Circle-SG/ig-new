# analytics

First-party event boundary and aggregate browser provider with runtime property
allowlisting. The optional collector stores daily totals only when
`ANALYTICS_DIRECTORY` is configured. No cookies, financial inputs or user IDs are
sent. See [analytics operations](../../docs/analytics.md) for limits and retention.
