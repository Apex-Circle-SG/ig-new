# Ginie boundary

Ginie is InsightGinie’s assistant, available at `/ask/`. The active application
implementation lives in `apps/web/src/lib/ask/` and uses the shared deterministic
calculator engines. This package directory retains its internal name for
compatibility. See [the Ginie implementation contract](../../docs/genie.md) for the
current provider and privacy behavior.
