# Web application

Next.js App Router frontend. Run from repository root with npm run dev or npm run build. Public calculator data are bundled; inputs stay in browser memory. See docs/local-development.md and docs/deployment.md.
