# Worker boundary

Reserved for validated dataset scheduling, scenario refresh notifications and opted-in monthly reports. No jobs are deployed or scheduled. A future job must be idempotent, preserve historical snapshots, expose failures and never send email without opt-in and unsubscribe support.
